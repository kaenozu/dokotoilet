# kaenozu/dokotoilet（きれいトイレ）コードレビュー（第2回）

- レビュー日: 2026-09-11（前回: 2026-09-04）
- 対象: 全ソース（`server.ts`、`server/` 32モジュール、`src/App.tsx` + コンポーネント 10 + `src/lib` 16モジュール、types、設定、スクリプト、CI、docs）を全文確認。`REVIEW_ja.md`（前回指摘）の全項目の実施状況を実コードで検証した。
- 検証: `bun run lint` / `bun run test` / `bun run build` を実機で実行済み

---

## 1. 概要

公衆トイレの「きれい度」を地図で共有するアプリ「きれいトイレ（どこトイレ）」の第2回レビュー。前回レビュー（2026-09-04）から約1週間で、**指摘した全 High / Medium 項目が検証可能な形で解消**していた。テストは 55本（5ファイル）→ **511本（42ファイル）** に増加。

**検証結果（2026-09-11 時点）**

- `bun run lint`（tsc --noEmit）→ ✅
- `bun run test`（vitest）→ ✅ **511/511 成功（42ファイル）**。fast-check のファジング、unicode オーディット、ルーターファズ、persistence / cache / migration スイートを含む
- `bun run build` → ✅（クライアント 594.9kB / gzip 115.9kB。react / leaflet / icons の vendor 分割済み。Vite の 500kB 超過警告は継続。サーバー 619.2kB）

**前回からの構造変化（主要なもの）**

| 領域 | 変化 |
|---|---|
| 永続化 | `CommunityStore` 契約の下に JSON バックエンド（atomic write + クロスプロセスファイルロック）と Firestore バックエンド（トランザクション集計）を実装。 discriminated-union の backend 設定で切替 |
| モデレーション | 管理用 API + ops ツール（community-ops）を追加 |
| 外部施設 | `osm-*` / `google-*` / `od-*` の口コミをサーバー共有（externalReviews v2） |
| クライアント状態 | localStorage はユーザーデルタのみ（起動時は常に最新シードとマージ）+ OSM 取得分は上限付きキャッシュに分離 |
| 共有ロジック | 施設ID（NFC正規化）、グレード判定、テキストポリシー等が `src/lib` / `server/shared` に純関数として集約 |

---

## 2. 前回レビュー（REVIEW_ja.md）の実施状況 — 全項目検証済み

### 2.1 🔴 高優先度 → すべて解消

| 項目 | 実施状況（2026-09-11 検証） |
|---|---|
| H1. Google 口コミ本文の転載 | **解消**。`googleSeed.ts` に引用本文なし（「より引用」0件）。取込スクリプトは `reviewExcerpts` を破棄し README / docs に転載禁止規約あり |
| H2. 永続化・モデレーション経路 | **解消**。Firestore バックエンド実装、`COMMUNITY_BACKEND` で明示切替（本番は未指定時起動拒否）、管理 API + ops ツール、`.env.example` に SALT 固定の警告記載 |
| H3. LICENSE / データライセンス | **解消**。LICENSE 追加、README にデータライセンス節、CI ワークフロー追加 |

### 2.2 🟠 中優先度（バグ）→ すべて解消

| 項目 | 実施状況（2026-09-11 検証） |
|---|---|
| M1. 未使用のフィルタ状態 | **解消**。未使用フィールド削除、絞り込み・ソートは `src/lib/filter.ts` の純関数に集約（テスト付き） |
| M2. 「清潔度順にソート」が嘘 | **解消**。評価済み→実測降順、未評価→推定降順の決定的ソートを実装し文言と一致 |
| M3. 自動生成レビューの不整合 | **解消**。`reviewCount: 0` に統一、201 応答のサーバー版を正として state 同期 |
| M4. soap / paper チェックボックス | **解消**。未配線フィールドは UI から削除する方針で実施（便座確認も含め除去） |
| M5. 外部施設の口コミがローカル限定 | **解消**。サーバー v2 が ID 形式で外部施設の口コミを受付・共有、クライアントは起動時・投稿成功時に重畳（`src/lib/externalReviews.ts`） |
| M6. localStorage にシード固定 | **解消**。ユーザーデルタのみ保存（`kirei-toilet-delta-v1`）+ 常時マージ + OSM キャッシュ分離 + 旧キー自動移行（`src/lib/localDeltas.ts`） |
| M7. 細かいバグ | **ほぼ解消**。OSM 失敗トーストは失敗文言に修正済み、`/api/osm/toilets` の lat / lng / radius は範囲・有限値チェック実装済み（1..3000 等、400 を返す）。楽観投票の巻き戻し等の残り細項目は今回の再確認対象外（flag 付きで残すなら今後の課題） |

### 2.3 🟡 低優先度 → ほぼ解消

| 項目 | 実施状況（2026-09-11 検証） |
|---|---|
| P1. AI Studio テンプレートの残骸 | **解消**。`package.json` name は `dokotoilet`、`metadata.json` は `majorCapabilities: []`、`.env.example` は実変数（COMMUNITY_SALT / COMMUNITY_BACKEND / COMMUNITY_STORE_PATH）と一致、tsconfig の不要オプション削除済み。`DISABLE_HMR` はスキャフォールド残骸ではなく、意図と説明付きの運用機能として整理されている |
| P2. コード配置・重複 | **解消**。`isEvaluated` / `getGradeColor` は `src/lib/grade.ts` に移動、カテゴリ→facilityType 変換は `facilityTypeForCategory` に集約、平均スコア計算はサーバーを正として共有ヘルパー化 |
| P3. マーカー再生成 | **解消**。`onSelectToilet` は ref 経由で参照し、マーカー effect の依存は `[toilets, selectedToilet?.id]` に限定。詳細パネル開閉時の `invalidateSize` も layoutKey 監視で実装済み |
| P4. バンドルサイズ | **一部解消**。`manualChunks` で react / leaflet / icons を vendor 分割（627kB → 594.9kB / gzip 148.6kB → 115.9kB）。メインチャンクはまだ 500kB 超過のため警告継続（後述 4.1） |
| P5. クエリ検証 | **解消**。M7 と同じ修正（範囲外は 400） |

---

## 3. 今回のレビューで新たに確認・対応した問題（2026-09-05 以降に修正済み）

本レビュー過程で新規に発見し、すでに修正・テスト済みのもの。コードは現時点で修正後の状態。

### 3.1 🔴 未評価のコミュニティ登録施設で画面全体が白くなるクラッシュ（修正済み）

**問題**: `AddToiletModal` が作るコミュニティ登録施設（および `POST /api/community/toilets` が保存するもの）は `cleanlinessScore / equipmentScore = null` の「未評価」状態で始まる。`displayGrade` がそのまま `{ grade: null, score: null }` を返し、消費側が null ガードなしで `.toFixed(1)` を呼んでいたため、**未評価施設を詳細表示した瞬間に TypeError → ErrorBoundary もないので React ツリー全体がアンマウント → 真っ白**。アプリの主たる書き込み経路（最初の1件登録）が必ず踏む形だった。プローブスクリプトで再現確認済み。

- `ToiletDetails.tsx`: `shown.score.toFixed(1)` でクラッシュ（サブスコアバーの `toFixed` も同様）
- `ToiletMap.tsx`: マーカーHTMLにリテラル `null` が出力
- `ToiletList.tsx`: `Number(null).toFixed(1)` で誤解を招く `0.0` 表示

**修正方針（実施済み）**:

- `src/lib/grade.ts`: `GradeDisplay.grade / score` を nullable に拡張し、JSDoc で「null は未評価として扱う契約」を明記。以後の消費側はコンパイラが null 経路の処理を強制する
- `ToiletDetails.tsx`: スコアは `–`、プレートは `?` と未評価色、ツールチップは「未評価（口コミの投稿で実測に更新されます）」。サブスコアバーは未評価時に**非表示**（0.0 の棒を描くと「実測0点」と誤読されるため。未確認とゼロを混同しないという本アプリのデータ哲学に合わせた）
- `ToiletList.tsx` / `ToiletMap.tsx`: `?` プレート + 未評価ラベル。マーカーの title は「（未評価・口コミ募集中）」
- `src/lib/filter.ts`: 副次的発見。S・A級フィルタで `null < 4.0` が `false` になるため**未評価施設がフィルタを素通れしていた**。null チェック付き比較に修正（未評価は除外）
- 回帰テスト: `grade.test.ts`（未評価 shape で `{grade:null, score:null}`）、`filter.test.ts`（S・A級から除外される）、コンポーネント SSR テスト（クラッシュしない・`0.0` もリテラル `null` も画面に出ない）

### 3.2 🟠 JSON / Firestore バックエンドで重複投稿ガードの判定が乖離（修正済み）

**問題**: 重複レビューガード（24h）の正規化ルールがバックエンドごとに二重実装だった。

- JSON バックエンド: コメントを正規化（trim + 空白圧縮 + 小文字化）して比較
- Firestore バックエンド: **生コメント**をそのままハッシュ（`dedupId`）

同じ「`"clean toilet"` 投稿後に `"  CLEAN  "` を投稿する」スパムが、JSON では検知され Firestore では素通れする。設計思想（「1実装を共有（二重実装の再分裂防止）」）に反する状態だった。

**修正方針（実施済み）**:

- **`server/shared/dedup.ts`（新規）** に正規化とキー導出を一本化: `normalizeDedupText`（JSON の歴史的ルールを正とする）、`reviewDedupId`（`sha256(facilityId|ipHash|normalizedComment)`）、`dedupCommentHash`（dedup ドキュメントには正規形のハッシュのみ保存）、`normalizeReportReason`（通報の重複比較も同ルール）
- `server/firestoreCommunityStore.ts`: ローカルの `dedupId` / `normalizeReportText` を削除し共有ヘルパーへ
- `server/community.ts`: JSON 側も共有正規化に統一（挙動は変更なし）
- `server/communityMigrationPlan.ts`: 同一キー式を使用。加えて、正規化導入前に書かれた旧 JSON レビュー（同一 facility+IP で `"clean"` と `"CLEAN"` 等）が移行時に同一 dedup ドキュメント ID に衝突するため、**`validUntil` が最も新しいドキュメントを残す決定的 collapse** を実装（冪等な `set` で片方が静かに消えるのを防ぐ）
- `docs/durable-community-backend.md`: dedupKey の仕様を `exactComment` → 正規化コメントに更新
- テスト: `server/shared/dedup.test.ts`（新規8件、キー式の独立検算と変種衝突プロパティを含む）、Firestore ストアに変種拒否の回帰テスト、移行計画にキー式一致と collapse のテスト

### 3.3 🟠 `@types/react` が欠落し、React の型チェックが全体的に無効だった（修正済み）

**問題**: `package.json` に `@types/react` / `@types/react-dom` が存在せず、react 19 本体は型定義を同梱しないため、**全ファイルの `react` import が暗黙 `any`** だった（tsconfig に `strict` / `noImplicitAny` がないため検知されない）。コンポーネントの props の型不整合・誤用がコンパイル時に一切検出できない状態で、lint（tsc --noEmit）の実効性を大きく損なっていた。ErrorBoundary（class コンポーネント）を追加した際に `React.Component` の解決失敗として顕在化し発覚。

**修正方針（実施済み）**: `@types/react@^19` / `@types/react-dom@^19` を devDependencies に追加。既存コードは追加型エラーなしで通過（= これまでのコードも暗黙 any に依存した壊れ方をしていなかったことの裏取りにもなる）。今後は props の型検査が有効になる。

### 3.4 防御強化: ErrorBoundary の導入（実施済み）

3.1 のような「1件の不正データ → 全画面白」を構造的に封じるため、ErrorBoundary を追加した。

- **`src/components/ErrorBoundary.tsx`（新規）**: class コンポーネント（関数コンポーネントは子のレンダー例外を捕捉できないため）。`getDerivedStateFromError` は状態確保のみ、副作用（コンソールへの1行ログ）は `componentDidCatch` に分離。fallback には「再表示を試す」（`setState` で再試行）と「端末データを初期化して再読み込み」の2ボタン
- **クラッシュループ対策**: 壊れた localStorage デルタが原因のクラッシュは、単純な `location.reload()` だと起動時に同じデータを拾って即再クラッシュする。2つ目のボタンは `clearStoredUserData()`（`src/lib/localDeltas.ts` に新規追加。ユーザーデルタと OSM キャッシュのみ削除）を通してからリロードする。失うのは「この端末のみの未同期分」だけであり、UI 文言でも明示
- **配置**: `App.tsx` の一覧パネル / 地図 / 詳細パネルの3リージョンに個別の境界（`resetKey` で選択施設の切替時に自動復帰を試みる）。`main.tsx` の最上位に最後の砦としてアプリ全体の境界。1リージョンの障害が他リージョンに波及しない
- **テスト**: ErrorBoundary はクライアント専用ライフサイクルのため SSR（`renderToStaticMarkup`）では検証できない（境界は発火せず例外が伝播する）。このため `happy-dom` を devDependencies に追加し、このテストファイルのみ実DOMマウント（`createRoot` + `act`）で検証: 子の正常表示、fallback への切替（例外メッセージの UI 漏れなし）、非 Error 例外の正規化、再試行ボタンの挙動、運用ログの出力

### 3.5 補足: 型の Nullable 化による設計上の利点

3.1 / 3.3 の結果、`GradeDisplay` の null 経路は「型で表現され、コンパイラが消費側の処理を強制し、実DOMテストが挙動を固定する」という三層構えになった。未評価（`reviewCount === 0`）と低評価（実測 0 点近傍）を混同しないという本アプリのデータ哲学が、表示・フィルタ・型・テストの一貫した整備として落ち着いている。

--- 

## 4. 残る改善点（低優先度）

### 4.1 メインチャンクが still 500kB 超過

vendor 分割後もメインチャンクは 594.9kB（gzip 115.9kB）で Vite 警告が継続。leaf の描画ライブラリは分離済みのため、次の一手はページ/モーダル単位の遅延ロード（`React.lazy` + `Suspense`。ReviewModal / AddToiletModal / 詳細パネルが候補）か、シードデータ（約 250 施設分）の chunk 分離。初回ロードの帯域が厳しい環境（モバイル）を考えると gzip 115.9kB は実用域だが、シード成長とともに再燃しやすい。

### 4.2 前回 M7 の残りの細項目の確認

楽観投票のサーバー応答による巻き戻し、オフライン投票の再送は、今回の検証スコープの外（`src/lib/helpfulVote.ts` が存在し分離されていることまでは確認）。残すなら「未同期操作の再送キュー」として localDeltas 側に設計を寄せるのが自然。

### 4.3 運用メモ

- `dev.log` / `dev.err.log` はワーキングツリーに残ることがあるが、`*.log` が `.gitignore` 済みで git 追跡もない（`git ls-files` で確認）。問題なし
- `data/` は `data/community.json` のみ git 管理（意図的。`.gitignore` に運用方針コメントあり）
- `vite.config.ts` の `DISABLE_HMR` はエージェント編集時の CPU 抑制として意図的に残す機能（コメント明記済み）。CDN 利用時に再検討したい Cookie / CSP の設定はコード内コメントで注記済み

---

## 5. 総評

**前回レビューから1週間で、High / Medium / Low の全指摘が「テストと実装で裏取りされた形」で解消されており、フォロースルーの規律は極めて高い。** 55本 → 511本へのテスト増強、二重実装の一本化（グレード判定・施設ID・正規化・重複ガード）、層分離（CommunityRepository 契約 + discriminated-union バックエンド）、atomic write + ファイルロック、NFC 正規化ID のクライアント/サーバー共有、実測/調査/推定の出典を UI まで貫く誠実さなど、設計として守るべきものが一貫して守られている。

今回のレビューでも、未評価施設の白画面クラッシュ（主経路上の実害）、重複ガードのバックエンド間乖離、`@types/react` 欠落という型チェックの空洞の3点を発見したが、いずれもすでに修正済みで、回帰テストと ErrorBoundary による構造的防御まで整っている。lint / test / build はすべてグリーン。

公開・運用に向けて残るのは 4.1（バンドル）と 4.2（投票の細項目）程度で、いずれもブロッカーではない。**現時点で MVP としての品質基準は満たしていると評価する。**

優先順位の目安:

1. なし（ブロッカーは解消済み）。次の機能開発を進めてよい状態
2. 暇なときに 4.1 の遅延ロード、4.2 の投票再送
3. Firestore 本番切替時は `docs/durable-community-backend.md` の migration / parity / rollback 手順に従う（ドキュメント整備済み）

---

*このレビューはリポジトリの全ソースを確認し、`bun run lint` / `bun run test` / `bun run build` を実行した上で作成しています。新規発見項目（3.x）はプローブスクリプト・テストによる再現確認済みで、修正コードも検証済み。*
